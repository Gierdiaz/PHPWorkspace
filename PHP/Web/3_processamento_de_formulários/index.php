<?php
/* 
- forms processing
- We will perform form processing in two ways: via GET and via POST;
- With GET we will process the parameters that come in the query string, that is, in the url;
- With the POST we will process the information that comes in the request, these do not appear in the URL;
- Usage examples: Queries => GET, User Registration => POST
 */
?>